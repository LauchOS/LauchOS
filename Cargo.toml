[package]
name = "lauch_os"
version = "0.1.0"
edition = "2021"

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic

[dependencies]
